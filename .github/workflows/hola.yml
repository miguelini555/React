name: Hola Mundo

on:
  push:
  workflow_dispatch:

jobs:
  hola:
    runs-on: ubuntu-latest
    steps:
      - name: Saludar
        run: echo  "Hola Mundo!"

      - name: Descargar Codigo
        uses: actions/checkout@v4
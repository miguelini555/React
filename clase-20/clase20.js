"use strict";
const Saludo = (props) => {
    return Hola;
    {
        props.nombre;
    }
    /p>;
};
render(nombre, "Juan" /  > );
if (props.nombre === "Juan") {
    return Hola;
    {
        props.nombre;
    }
    /p>;
}
return No;
eres;
Juan < /p>;
const numeros = [10, 20, 30, 40, 50, 60];
const dobles = numeros.map((item) => { return item * 2; });
